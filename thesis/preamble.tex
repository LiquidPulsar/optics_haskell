\usepackage[
  backend=biber,
  style=vancouver,
  natbib=true
]{biblatex}
\addbibresource{references.bib}

% --- amsthm must load before newtxmath (which lhs2tex-preamble pulls in) ---
\usepackage{amsthm}

% --- lhs2tex bootstrap (must come first) ---
% Fires the ReadOnlyOnce blocks in the preamble so \usepackage calls inside
% generated chapter files are suppressed when \include'd after \begin{document}.
\input{generated/lhs2tex-preamble}
\setlength{\mathindent}{1em}   % reduce code-block left indent (default: \leftmargini ≈ 2.5em)
\AtBeginDocument{%
  \setlength{\abovedisplayskip}{5pt plus 2pt minus 2pt}%
  \setlength{\belowdisplayskip}{3pt plus 2pt minus 2pt}%
}

% --- Encoding & fonts ---
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}

% --- Math & symbols ---
\usepackage{amsmath}
\let\Bbbk\relax            % newtxmath (via lhs2tex) already defines \Bbbk
\usepackage{amssymb}
% stmaryrd already loaded by lhs2tex's polycode.fmt

% --- Theorems ---
\newtheorem{theorem}{Theorem}[chapter]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{example}[theorem]{Example}
\newtheorem{remark}[theorem]{Remark}

% --- Page layout ---
\usepackage[a4paper, margin=2.5cm]{geometry}
\usepackage{setspace}
\setstretch{1.3}

% --- Graphics ---
\usepackage{graphicx}
\graphicspath{{figures/}}

% --- Tables ---
\usepackage{booktabs}

% --- Diagrams ---
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta, calc}
\tikzset{
  paralens/.style={draw, thick, minimum width=1.4cm, minimum height=0.9cm},
  wire/.style={->, >=Stealth, thick},
  inwire/.style={->, >=Stealth, semithick, gray!70!black}
}

% --- Chapter/section title spacing ---
\usepackage{titlesec}
\titleformat{\chapter}[display]
  {\normalfont\huge\bfseries}{\chaptertitlename~\thechapter}{16pt}{\Huge}
\titlespacing*{\chapter}{0pt}{0pt}{24pt}
\titlespacing*{\section}{0pt}{1.5ex plus .5ex minus .3ex}{0.8ex plus .2ex}
\titlespacing*{\subsection}{0pt}{1.2ex plus .4ex minus .2ex}{0.5ex plus .1ex}
\titlespacing*{\subsubsection}{0pt}{1.0ex plus .3ex minus .2ex}{0.3ex plus .1ex}

% --- References & links (load last) ---
\usepackage[hidelinks]{hyperref}


\usepackage{parskip}